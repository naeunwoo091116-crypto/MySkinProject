# 얼굴 감지 기능 가이드 (MediaPipe)

## 🎯 개요

**Google MediaPipe Face Detection**을 사용하여 얼굴이 없는 사진을 업로드하면 분석이 거부되고 사용자에게 오류 메시지를 표시합니다.

---

## ✅ 검증 기준 (5단계) - **MediaPipe AI 얼굴 감지**

### 1단계: 이미지 크기 검증
```python
if width < 100 or height < 100:
    return False, "이미지가 너무 작습니다 (최소 100x100)"
```
- **최소 크기:** 100 x 100 픽셀
- **목적:** 너무 작은 이미지는 얼굴 분석 불가능

---

### 2단계: 이미지 밝기 검증
```python
mean_brightness = np.mean(img_array)

if mean_brightness < 20:
    return False, "이미지가 너무 어둡습니다"
if mean_brightness > 235:
    return False, "이미지가 너무 밝습니다"
```
- **밝기 범위:** 20 ~ 235 (0-255 스케일)
- **목적:** 너무 어둡거나 밝으면 얼굴 인식 불가능

---

### 3단계: **MediaPipe 얼굴 감지** ⭐ 핵심 기능
```python
mp_face_detection = mp.solutions.face_detection

with mp_face_detection.FaceDetection(
    model_selection=0,  # 0: 2m 이내 (셀카), 1: 5m 이내
    min_detection_confidence=0.5  # 50% 신뢰도 임계값
) as face_detection:

    results = face_detection.process(img_array)

    if not results.detections:
        return False, "얼굴이 감지되지 않았습니다"
```
- **AI 모델:** Google MediaPipe Face Detection
- **신뢰도 임계값:** 50% (0.5)
- **최적화:** 셀카 촬영에 특화 (2m 이내)
- **정확도:** 매우 높음 (다양한 각도, 조명 환경 지원)

---

### 4단계: 얼굴 신뢰도 검증
```python
max_confidence = max(detection.score[0] for detection in results.detections)

if max_confidence < 0.5:  # 50% 미만
    return False, f"얼굴 신뢰도가 낮습니다 ({max_confidence*100:.0f}%)"
```
- **임계값:** 50% 이상
- **목적:** 불확실한 감지 결과 거부

---

### 5단계: 얼굴 크기 검증
```python
bbox = main_face.location_data.relative_bounding_box
face_area = bbox.width * bbox.height

if face_area < 0.05:  # 이미지의 5% 미만
    return False, "얼굴이 너무 작습니다. 더 가까이서 촬영해주세요"
```
- **최소 면적:** 이미지의 5% 이상
- **목적:** 너무 작은 얼굴은 정확한 분석 불가

---

## 🔬 작동 원리 (MediaPipe AI)

### ✅ 정상적인 얼굴 사진
```
이미지 업로드
  ↓
1️⃣ 크기 확인: 1280x720 ✅
  ↓
2️⃣ 밝기 확인: 평균 128 ✅
  ↓
3️⃣ MediaPipe 얼굴 감지:
  - 딥러닝 모델 실행
  - 얼굴 1개 감지 ✅
  - 신뢰도: 95.3%
  - Bounding Box: x=0.35, y=0.20, w=0.30, h=0.45
  ↓
4️⃣ 신뢰도 검증: 95.3% > 50% ✅
  ↓
5️⃣ 얼굴 크기 검증:
  - 면적: 0.30 × 0.45 = 13.5%
  - 13.5% > 5% ✅
  ↓
✅ 얼굴 검증 통과 → 분석 시작
```

### ❌ 얼굴이 없는 사진 (예: 풍경)
```
이미지 업로드
  ↓
1️⃣ 크기 확인: 1920x1080 ✅
  ↓
2️⃣ 밝기 확인: 평균 150 ✅
  ↓
3️⃣ MediaPipe 얼굴 감지:
  - 딥러닝 모델 실행
  - 얼굴 0개 감지 ❌
  ↓
❌ "얼굴이 감지되지 않았습니다"
```

### ❌ 얼굴이 너무 작은 사진
```
이미지 업로드
  ↓
1️⃣ 크기 확인: 1920x1080 ✅
  ↓
2️⃣ 밝기 확인: 평균 145 ✅
  ↓
3️⃣ MediaPipe 얼굴 감지:
  - 얼굴 1개 감지 ✅
  - 신뢰도: 78.5%
  ↓
4️⃣ 신뢰도 검증: 78.5% > 50% ✅
  ↓
5️⃣ 얼굴 크기 검증:
  - 면적: 0.08 × 0.12 = 0.96%
  - 0.96% < 5% ❌
  ↓
❌ "얼굴이 너무 작습니다. 더 가까이서 촬영해주세요"
```

---

## 📱 사용자 경험

### 시나리오 1: 정상 얼굴 사진
1. 얼굴 사진 업로드
2. AI 분석 실행
3. ✅ 정상적으로 분석 결과 표시

### 시나리오 2: 얼굴 없는 사진 (풍경, 사물)
1. 풍경 사진 업로드
2. MediaPipe 분석 실행
3. 🔴 Toast 메시지: **"얼굴이 감지되지 않았습니다"**
4. 분석 중단

### 시나리오 3: 얼굴이 너무 작은 사진
1. 멀리서 찍은 사진 업로드
2. MediaPipe 분석 실행
3. 🔴 Toast 메시지: **"얼굴이 너무 작습니다. 더 가까이서 촬영해주세요"**

### 시나리오 4: 너무 어두운 사진
1. 어두운 셀카 업로드
2. 밝기 검사 실행
3. 🔴 Toast 메시지: **"이미지가 너무 어둡습니다"**

---

## 🔧 임계값 조정

### 신뢰도 임계값 변경

현재: **50%** (권장값)

**더 엄격하게** (false positive 감소):
```python
# app.py 385-388번째 줄
with mp_face_detection.FaceDetection(
    model_selection=0,
    min_detection_confidence=0.7  # 50% → 70%로 상향
) as face_detection:
```

**더 관대하게** (false negative 감소):
```python
with mp_face_detection.FaceDetection(
    model_selection=0,
    min_detection_confidence=0.3  # 50% → 30%로 하향
) as face_detection:
```

### 얼굴 크기 임계값 변경

현재: **5%** (권장값)

**더 엄격하게** (더 큰 얼굴만 허용):
```python
# app.py 423-425번째 줄
if face_area < 0.10:  # 5% → 10%로 상향
    return False, "얼굴이 너무 작습니다. 더 가까이서 촬영해주세요"
```

**더 관대하게** (작은 얼굴도 허용):
```python
if face_area < 0.02:  # 5% → 2%로 하향
    return False, "얼굴이 너무 작습니다. 더 가까이서 촬영해주세요"
```

### 감지 범위 변경

```python
# 원거리 사진도 감지 (5m 이내)
with mp_face_detection.FaceDetection(
    model_selection=1,  # 0 → 1로 변경
    min_detection_confidence=0.5
) as face_detection:
```

---

## 🧪 테스트 방법 (필수!)

### 1단계: 서버 재시작
```bash
# 기존 서버 중지 (Ctrl+C)
# MediaPipe 적용을 위해 재시작
python app.py
```

### 2단계: 얼굴 사진 테스트 (정상 동작 확인)
1. 브라우저에서 `http://localhost:5000` 접속
2. **실제 얼굴 셀카** 업로드
3. 예상 결과:
   - ✅ "AI 분석 실행" 버튼 표시
   - ✅ 정상 분석 진행
   - ✅ 서버 로그: "MediaPipe: 1개의 얼굴 감지"

### 3단계: 풍경 사진 테스트 (거부 확인)
1. **풍경 사진**(산, 바다, 건물 등) 업로드
2. 예상 결과:
   - ❌ 빨간색 Toast: "얼굴이 감지되지 않았습니다"
   - ❌ 분석 중단
   - ❌ 서버 로그: "MediaPipe: 얼굴이 감지되지 않았습니다"

### 4단계: 사물 사진 테스트 (거부 확인)
1. **사물 사진**(책상, 컴퓨터, 음식 등) 업로드
2. 예상 결과:
   - ❌ "얼굴이 감지되지 않았습니다" 메시지

### 5단계: 멀리서 찍은 사진 테스트
1. **얼굴이 작게 나온 사진** 업로드
2. 예상 결과:
   - ❌ "얼굴이 너무 작습니다. 더 가까이서 촬영해주세요"

### 6단계: 서버 로그 확인
터미널에서 다음 정보 확인:

**정상 로그:**
```
🔍 [MediaPipe 얼굴 검증 시작]
📏 이미지 크기: 1280x720
💡 평균 밝기: 128.5
✅ MediaPipe: 1개의 얼굴 감지
   얼굴 #1 신뢰도: 95.3%
   위치: x=0.35, y=0.20, w=0.30, h=0.45
✅ 얼굴 검증 통과 (신뢰도: 95.3%, 면적: 13.5%)
```

**비정상 로그 (풍경):**
```
🔍 [MediaPipe 얼굴 검증 시작]
📏 이미지 크기: 1920x1080
💡 평균 밝기: 142.3
❌ MediaPipe: 얼굴이 감지되지 않았습니다
```

---

## 📊 API 응답

### 성공 (얼굴 있음)
```json
{
  "overall_score": 78,
  "regions": { ... },
  "recommendation": { ... }
}
```

### 실패 (얼굴 없음)
```json
{
  "error": "invalid_image",
  "message": "얼굴이 감지되지 않았습니다",
  "details": "얼굴이 포함된 사진을 업로드해주세요."
}
```
**HTTP 상태 코드:** `400 Bad Request`

---

## 🎨 프론트엔드 처리

```javascript
// 얼굴 감지 실패 처리
if (response.status === 400) {
    const errorData = await response.json();
    if (errorData.error === 'invalid_image') {
        showLoading(false);
        showToast(errorData.message, "error");  // 빨간 토스트
        return;  // 데모 모드 진입하지 않음
    }
}
```

**특징:**
- ✅ 사용자에게 명확한 오류 메시지
- ✅ 데모 모드로 진입하지 않음 (잘못된 데이터 방지)
- ✅ 재시도 가능 (새 사진 선택)

---

## 📝 요약

| 검증 단계 | 목적 | 임계값 |
|----------|------|--------|
| 1. 이미지 크기 | 최소 해상도 보장 | 100x100 이상 |
| 2. 밝기 검사 | 적절한 노출 확인 | 20~235 (0-255) |
| **3. MediaPipe AI** ⭐ | **얼굴 감지** | **신뢰도 50% 이상** |
| 4. 신뢰도 검증 | 불확실한 결과 거부 | 50% 이상 |
| 5. 얼굴 크기 | 충분한 얼굴 크기 | 이미지의 5% 이상 |

**장점:**
- ✅ **최고 수준 정확도** - Google MediaPipe AI 모델
- ✅ **다양한 각도 지원** - 정면, 측면, 약간 기울어진 얼굴도 감지
- ✅ **조명 강건성** - 다양한 조명 환경에서 작동
- ✅ **빠른 속도** - 최적화된 딥러닝 모델 (< 100ms)
- ✅ **여러 얼굴 감지** - 동시에 여러 명 감지 가능
- ✅ **상세한 정보** - Bounding box, 신뢰도, 얼굴 크기 제공
- ✅ **프로덕션 검증** - Google 제품에 실제 사용

**단점:**
- ⚠️ 추가 라이브러리 필요 (mediapipe)
- ⚠️ 극단적 각도(완전한 옆모습)는 감지 어려움
- ⚠️ 가려진 얼굴(마스크, 선글라스)은 신뢰도 낮음

---

## 🔍 디버깅

### 정상적인 로그:
```
==================================================
📸 [AI 분석 시작] 사용자: user_abc123 | 이미지: (1280, 720)
==================================================

   🔍 [MediaPipe 얼굴 검증 시작]
   📏 이미지 크기: 1280x720
   💡 평균 밝기: 128.5
   ✅ MediaPipe: 1개의 얼굴 감지
      얼굴 #1 신뢰도: 95.3%
      위치: x=0.35, y=0.20, w=0.30, h=0.45
   ✅ 얼굴 검증 통과 (신뢰도: 95.3%, 면적: 13.5%)

✅ 얼굴 검증 통과
```

### 비정상 로그 (풍경):
```
==================================================
📸 [AI 분석 시작] 사용자: user_abc123 | 이미지: (1920, 1080)
==================================================

   🔍 [MediaPipe 얼굴 검증 시작]
   📏 이미지 크기: 1920x1080
   💡 평균 밝기: 142.3
   ❌ MediaPipe: 얼굴이 감지되지 않았습니다
❌ 얼굴 검증 실패: 얼굴이 감지되지 않았습니다
```

---

## 🚀 기술 상세

### MediaPipe Face Detection 모델

**모델 아키텍처:**
- BlazeFace: 모바일 GPU에 최적화된 경량 얼굴 감지 모델
- SSD (Single Shot Detector) 기반
- 128x128 입력 해상도
- MobileNetV2 백본

**두 가지 모델:**
1. **Short-range (model_selection=0)**
   - 2m 이내 얼굴 감지
   - 셀카, 얼굴 클로즈업에 최적화
   - 더 정확한 얼굴 landmark

2. **Full-range (model_selection=1)**
   - 5m 이내 얼굴 감지
   - 그룹 사진, 원거리 촬영
   - 더 넓은 감지 범위

### 출력 정보

```python
detection.score[0]  # 신뢰도 (0.0 ~ 1.0)
detection.location_data.relative_bounding_box
  - xmin: 좌상단 x 좌표 (0.0 ~ 1.0)
  - ymin: 좌상단 y 좌표 (0.0 ~ 1.0)
  - width: 박스 너비 (0.0 ~ 1.0)
  - height: 박스 높이 (0.0 ~ 1.0)
```

---

## 📚 참고 자료

- [MediaPipe Face Detection 공식 문서](https://google.github.io/mediapipe/solutions/face_detection.html)
- [BlazeFace 논문](https://arxiv.org/abs/1907.05047)
- [MediaPipe GitHub](https://github.com/google/mediapipe)

---

## 💡 축하합니다! 🎉

**Google MediaPipe AI**를 사용한 프로덕션급 얼굴 감지 시스템이 적용되었습니다!
